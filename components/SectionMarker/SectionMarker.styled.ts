import { styled } from '@mui/material/styles';

export const SectionMarkerContainer = styled('div')({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  borderRadius: '50%',
  background: 'linear-gradient(135deg, rgba(219,123,123,1) 25%, rgba(164,63,58,1) 50%, rgba(122,18,15,1) 75%)',
  boxShadow: '0px 0px 10px 0px rgba(0,0,0,0.75)',
  minWidth: 100,
  width: 100,
  height: 100,
});
